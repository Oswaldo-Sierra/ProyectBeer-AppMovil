<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Color interno -->
    <solid android:color="@color/white"/>

    <!-- Borde -->
    <stroke
        android:width="15dp"
        android:color="#F7D98E"/>

    <!-- Bordes redondeados -->
    <corners android:radius="10dp"/>
</shape>
